$kind: http-request
url: "{{baseUrl}}/api/cobranzas/:id/generar-recibo"
method: POST
headers:
  Authorization: Bearer {{authToken}}
  X-Condominio-Id: "{{condominioId}}"
pathVariables:
  id: "1"
order: 4000
