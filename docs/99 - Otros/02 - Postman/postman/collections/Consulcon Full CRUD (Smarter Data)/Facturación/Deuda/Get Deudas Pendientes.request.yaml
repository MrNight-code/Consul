$kind: http-request
url: "{{baseUrl}}/api/Deuda/pendiente"
method: GET
headers:
  X-Condominio-Id: "{{condominioId}}"
  Content-Type: application/json
  Authorization: Bearer {{authToken}}
order: 1000
