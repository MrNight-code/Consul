$kind: http-request
description: Reabre un per├¡odo fiscal previamente cerrado.
url: "{{baseUrl}}/api/fiscal-periods/reopen"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{authToken}}
  X-Condominio-Id: "{{condominioId}}"
body:
  type: text
  content: |-
    {
      "year": 2026,
      "month": 1
    }
order: 2000
