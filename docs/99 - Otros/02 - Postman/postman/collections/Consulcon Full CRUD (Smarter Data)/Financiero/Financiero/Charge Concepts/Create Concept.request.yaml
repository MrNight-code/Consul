$kind: http-request
url: "{{baseUrl}}/api/FinancialConfig/concepts/{{condominioId}}"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{authToken}}
  X-Condominio-Id: "{{condominioId}}"
body:
  type: text
  content: |-
    {
        "name": "Expensas",
        "code": "EXP",
        "isRecurrent": true
    }
order: 2000
