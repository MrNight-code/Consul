$kind: http-request
url: "{{baseUrl}}/api/Contabilidad/autorizaciones"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{authToken}}
  X-Condominio-Id: "{{condominioId}}"
body:
  type: text
  content: |-
    {
        "descripcion": "Nivel de autorizaci\u00f3n test",
        "activo": true
    }
order: 2000
