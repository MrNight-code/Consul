$kind: http-request
url: "{{baseUrl}}/api/Tesoreria/formaspago"
method: POST
headers:
  Authorization: Bearer {{authToken}}
  X-Condominio-Id: "{{condominioId}}"
  Content-Type: application/json
body:
  type: text
  content: |-
    {
        "descripcion": "Transferencia QR",
        "codigo": "QR",
        "esBancarizado": true
    }
order: 2000
