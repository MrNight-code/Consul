$kind: http-request
url: "{{baseUrl}}/api/expenses/{{id}}/void"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{authToken}}
  X-Condominio-Id: "{{condominioId}}"
body:
  type: text
  content: |-
    {
        "reason": "Anulaci├│n por error en registro - Motivo valido > 10 chars"
    }
order: 5000
